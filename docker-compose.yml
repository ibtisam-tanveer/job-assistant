services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - jobassistant_mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: jobassistant

volumes:
  jobassistant_mongo_data:
