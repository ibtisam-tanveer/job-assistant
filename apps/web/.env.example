NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional. Same secret string as WEB_API_SECRET in services/api/.env when that is set.
# Use JOB_API_SECRET or WEB_API_SECRET here (either name works for server-side Next.js calls).
JOB_API_SECRET=
# WEB_API_SECRET=
